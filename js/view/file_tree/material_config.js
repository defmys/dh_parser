import {BaseConfig} from "./base_config";

export class MaterialConfig extends BaseConfig {
    prepareConfigContent(content) {
        return super.prepareConfigContent(content);
    }

    content(rawData) {
        return super.content(rawData);
    }
}